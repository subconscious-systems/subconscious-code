//! rc-tools: built-in tool implementations (§6).
//!
//! Not yet implemented. §16 starts here with `Read` + `ReadRegistry` (the
//! registry is what makes the Write/Edit "read before mutate" rule load-
//! bearing, §6.2/§6.3). M2 adds Write, Edit, Glob, Grep, and a foreground Bash.
