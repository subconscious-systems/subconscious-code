//! xtask: repository build tasks (§1).
//!
//! M0: no tasks yet. Will host schema generation and the `insta` snapshot
//! runner as the harness grows.
fn main() {
    eprintln!("xtask: no tasks defined yet (M0).");
}
